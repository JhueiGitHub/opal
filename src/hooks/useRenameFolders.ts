// root/src/hooks/useRenameFolders.ts

import { useRef } from 'react'

export const useRenameFolders = (folderId: string) => {
  const inputRef = useRef<HTMLInputElement | null>(null)
  const folderCardRef
}
